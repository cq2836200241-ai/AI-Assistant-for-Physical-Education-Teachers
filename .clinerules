# =========================
# CLINE RULES v2.5 (FULL SYSTEM)
# VSCode + Cline + DeepSeek + MCP
# =========================

# =========================
# 0. CORE PRINCIPLE（核心原则）
# =========================
稳定性 > 正确性 > 完整性 > 优化

目标：稳定修复问题，而不是重构或优化系统。

---

# =========================
# 1. EXECUTION FLOW（强制执行流程）
# =========================

所有任务必须按顺序执行：

1. 只读分析（理解问题，不修改）
2. filesystem MCP 定位相关文件
3. 必要时 Playwright MCP 复现 UI 问题
4. 所有 UI 信息必须结构化（见插件）
5. DeepSeek 进行纯文本推理
6. read_file 精确修改代码
7. 最小范围修复（1~3文件）
8. 验证修复结果（Playwright 或代码验证）

❗禁止跳过分析直接修改

---

# =========================
# 2. MCP TOOL DECISION RULE（工具选择规则）
# =========================

## UI / 前端问题
必须使用：
→ Playwright MCP（复现）
→ filesystem MCP（定位代码）

---

## 逻辑 / 后端问题
必须使用：
→ read_file（主）
→ filesystem MCP（辅助）

---

## 不确定问题
必须组合：
→ filesystem MCP + Playwright MCP（如涉及 UI）

---

## 项目结构问题
必须使用：
→ filesystem MCP

---

# =========================
# 3. DEBUG LOOP ENGINE（调试闭环）
# =========================

所有 bug 必须进入循环：

1. Playwright 复现（如 UI）
2. filesystem 定位
3. UI结构化（插件）
4. DeepSeek 推理
5. 最小修改
6. 再验证

最多循环 3 次：

超过 → 回到重新分析

---

# =========================
# 4. CHANGE PRINCIPLE（修改原则）
# =========================

- 只修 bug，不做优化
- 不改架构
- 不加依赖（除非必须）
- 不重写模块
- 不批量修改文件

---

# =========================
# 5. CONTEXT CONTROL（上下文控制）
# =========================

允许：
- 当前任务文件
- MCP 定位结果
- 直接依赖文件

禁止：
- 全项目扫描
- 无关目录遍历

---

# =========================
# 6. FILE & COMMAND SAFETY（安全规则）
# =========================

禁止：
- 删除文件
- rm -rf 等危险命令
- 修改 .env（除非明确要求）
- 大规模重命名

执行命令必须说明用途

---

# =========================
# 7. DEEPSEEK ROLE（模型职责）
# =========================

DeepSeek 只负责：

- bug原因推理
- 逻辑分析
- 修改方案生成

❗禁止：
- 视觉推理
- 猜 UI 状态

---

# =========================
# 8. FAILURE HANDLING（失败处理）
# =========================

如果连续失败 ≥ 2 次：

必须执行：

1. filesystem 重新定位
2. Playwright 重新复现
3. 清除所有假设
4. 重新进入 debug loop

---

# =========================
# 9. OUTPUT FORMAT RULE（输出规范）
# =========================

每次修改前必须说明：

- 问题总结
- 根因分析
- 修改文件范围
- 修改方案
- 风险评估

---

# =========================
# 10. PLUGINS（扩展模块）
# =========================

## =========================
## MCP VISUAL STRUCTURE PROTOCOL v1
## =========================

DeepSeek 不具备视觉能力，
所有 UI 必须由 Playwright MCP 转换为结构化文本。

---

### ① PAGE STATE（页面状态）
- url:
- route:
- loaded:

---

### ② UI COMPONENT STATE（组件状态）
- button:
  - name:
  - state: enabled / disabled / loading
- input:
  - value:
  - valid:
- modal:
  - open:

---

### ③ USER ACTION TRACE（用户行为）
1. click:
2. input:
3. submit:

---

### ④ RESULT STATE（结果）
- success / fail
- UI变化
- 预期 vs 实际

---

### ⑤ NETWORK STATE
- request url
- status code
- error

---

### ⑥ CONSOLE STATE
- errors
- warnings

---

# =========================
# END OF CLINE RULES v2.5
# =========================