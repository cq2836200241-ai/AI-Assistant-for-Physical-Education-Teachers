## Cline Rules (Engineering Stable Mode)

## 0. Core Goal（核心目标）
AI 的唯一目标是：稳定完成当前任务，不扩展、不重构、不发散。

优先级：
稳定性 > 正确性 > 完整性 > 优化

---

## 1. Task Execution Flow（强制执行流程）
所有任务必须按顺序执行：

1. 只读分析（理解问题，不修改代码）
2. 定位文件（找到真正相关代码）
3. 制定修改方案（说明改什么、为什么）
4. 执行最小修改（只改必要部分）
5. 验证影响范围

❗禁止跳过分析直接修改

---

## 2. Scope Control（修改范围控制）
严格限制：

- 每次最多修改 1~3 个文件
- 不允许跨模块扩展修改
- 不允许“顺手优化代码”
- 不允许重构整个结构

如果需要更多修改：
→ 必须拆分任务执行

---

## 3. Change Principle（修改原则）
所有代码修改必须遵守：

- 优先修复问题，而不是优化代码
- 不改变原有架构设计
- 不重写已有逻辑
- 不替换现有技术方案
- 不新增不必要依赖

---

## 4. Debug Discipline（调试纪律）
处理 bug 必须按顺序：

1. 复现问题
2. 定位文件
3. 分析原因
4. 提出方案
5. 执行修复
6. 检查是否引入新问题

❗禁止边改边猜

---

## 5. Context Control（上下文控制）
AI 只能使用：

- 当前任务相关文件
- 直接依赖文件

禁止：

- 扫描整个项目
- 读取无关目录
- 全局搜索后大范围修改

---

## 6. Dependency Safety（依赖安全）
- 默认禁止新增 npm 依赖
- 禁止升级大版本依赖
- 如必须新增：
  → 必须说明原因 + 为什么无法用现有方案

---

## 7. File Safety（文件安全）
禁止：

- 删除文件
- 批量重命名文件
- 随意移动目录结构
- 修改 .env 或敏感配置

---

## 8. Command Safety（命令安全）
执行 bash 命令必须：

- 说明用途
- 避免危险操作
- 禁止 rm -rf / reset --hard
- 不确定时必须暂停并询问

---

## 9. Output Requirement（输出规范）
每次执行修改前必须说明：

- 当前问题总结
- 修改范围
- 修改方案
- 风险评估

然后才允许执行代码修改

---

## 10. Anti Over-Engineering（防过度设计）
禁止行为：

- 主动重构系统
- 主动增加抽象层
- 主动拆分架构
- 主动优化非相关代码

---

## 11. Stability Rule（稳定性规则）
如果出现以下情况必须停止：

- 修改超过 3 次仍未解决问题
- 修改范围不断扩大
- 开始偏离原始问题

必须回到：重新分析阶段